# Exercise 1 — Swap Two Numbers Using Pointers

Write a function:

void swap(int *a, int *b);

Use it to swap two integers entered by the user.

Example:
Input: a = 5, b = 10  
Output: a = 10, b = 5